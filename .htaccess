<IfModule mod_rewrite.c>
    RewriteEngine On

    # Route requests through Laravel's public directory when the web root
    # is configured at the project root.
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
